import { App.Routing.Module } from './app.routing.module';

describe('App.Routing.Module', () => {
  it('should create an instance', () => {
    expect(new App.Routing.Module()).toBeTruthy();
  });
});
